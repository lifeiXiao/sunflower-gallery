include ../../leaf/_leaf.jade
include ../../cloud/_cloud.jade
mixin slice(month, position)
    -var verticalCloudAdjustment = Math.floor(Math.random() * 8) , horizontalCloudAdjustment = Math.floor(Math.random() * 8);
    .slice
        .left
            if position === 'left'
                +leaf(month)
            else
                |&nbsp;
            //+cloud(verticalCloudAdjustment, horizontalCloudAdjustment)
        .stem &nbsp;
        .right
            - verticalCloudAdjustment = Math.floor(Math.random() * 8);
            - horizontalCloudAdjustment = Math.floor(Math.random() * 8);
            if position === 'right'
                +leaf(month)
            else

                |&nbsp;
            //+cloud(verticalCloudAdjustment, horizontalCloudAdjustment)